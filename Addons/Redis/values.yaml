global:
  storageClass: "ebs-sc"
  imagePullSecrets: []

redis:
  image:
    registry: registry-1.docker.io
    repository: bitnami/redis
    tag: latest
    pullSecrets: []

  auth:
    enabled: true
    password: "password"

  sentinel:
    enabled: false
  metrics:
    enabled: false
